server:
  port: 80

#数据库配置
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password:
    url: jdbc:mysql://127.0.0.1:3306/blog
  jpa:
    show-sql: true

#日志配置
logging:
  file:
    max-size: 10MB
    path: /var/logs/myblog/